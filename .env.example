# 后台登录密码。部署到 Vercel 后必须设置。
ADMIN_PASSWORD=

# 可选。用来签发登录状态，不填就用 ADMIN_PASSWORD。
ADMIN_SECRET=

# 播放页上显示的店名
NEXT_PUBLIC_STORE_NAME=门店音乐

# 在 Vercel 里创建 Blob 存储并连接到项目后会自动写入，不用手填。
# BLOB_READ_WRITE_TOKEN=
